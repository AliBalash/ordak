services:
  gemini-ui-automation:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      BROWSER_HEADLESS: "true"
    volumes:
      - .:/app

